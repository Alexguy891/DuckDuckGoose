const path = require("path");

const nodeModules = path.resolve(`${__dirname}/../node_modules`);
const cwd = process.cwd();

const paths = {
  base: path.resolve(`${__dirname}/..`),
  lib: path.resolve(__dirname),
  bin: path.resolve(`${nodeModules}/.bin`),
  pluginTransformRuntime: path.resolve(
    `${nodeModules}/@babel/plugin-transform-runtime`
  ),
  presets: {
    env: path.resolve(`${nodeModules}/@babel/preset-env`)
  },
  src: cwd,
  dist: `${cwd}/dist`,
  tests: `${cwd}/tests`
};

module.exports = paths;
