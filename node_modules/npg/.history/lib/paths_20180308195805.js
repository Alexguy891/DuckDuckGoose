const path = require("path");

const nodeModulesPath = path.resolve(`${__dirname}/../node_modules`);
const npgNodeModulesPath = path.resolve(
  `${__dirname}/../node_modules/npg/node_modules`
);
const cwd = process.cwd();

const paths = {
  base: path.resolve(`${__dirname}/..`),
  lib: path.resolve(__dirname),
  bin: path.resolve(`${nodeModulesPath}/.bin`),
  plugins: {
    proposalClassProperties: path.resolve(
      `${npgNodeModulesPath}/@babel/plugin-proposal-class-properties`
    ),
    proposalObjectRestSpread: path.resolve(
      `${npgNodeModulesPath}/@babel/plugin-proposal-object-rest-spread`
    ),
    transformRuntime: path.resolve(
      `${npgNodeModulesPath}/@babel/plugin-transform-runtime`
    )
  },
  presets: {
    env: path.resolve(`${nodeModulesPath}/@babel/preset-env`)
  },
  src: cwd,
  dist: `${cwd}/dist`,
  tests: `${cwd}/tests`
};

module.exports = paths;
