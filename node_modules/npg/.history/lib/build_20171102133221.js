const paths = require("./paths");
const babel = require("./babel");

const build = function() {
  const args = [`--plugins=${paths.pluginTransformRuntime}`];
  babel(args);
};

module.exports = build;
