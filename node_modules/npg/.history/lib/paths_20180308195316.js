const path = require("path");

const nodeModulesPath = path.resolve(`${__dirname}/../node_modules`);
const cwd = process.cwd();

const paths = {
  base: path.resolve(`${__dirname}/..`),
  lib: path.resolve(__dirname),
  bin: path.resolve(`${nodeModulesPath}/.bin`),
  pluginProposalClassProperties: path.resolve(
    `${nodeModulesPath}/@babel/plugin-proposal-class-properties`
  ),
  pluginProposalObjectRestSpread: path.resolve(
    `${nodeModulesPath}/@babel/plugin-proposal-object-rest-spread`
  ),
  pluginTransformRuntime: path.resolve(
    `${nodeModulesPath}/@babel/plugin-transform-runtime`
  ),
  presets: {
    env: path.resolve(`${nodeModulesPath}/@babel/preset-env`)
  },
  src: cwd,
  dist: `${cwd}/dist`,
  tests: `${cwd}/tests`
};

module.exports = paths;
